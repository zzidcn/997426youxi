积分问题最终诊断结论（2026-08-25）

线上实测证据：
1. POST /score 提交成功 {ok:true, rank:2} —— 写入链路正常
2. leaderboard 返回两条游客记录（789 / 555），且 555 是缓存过期后才出现
   —— 说明线上插件仍是 v1.2.3 之前的旧版：
   - 无 ip_hash 聚合（同一游客多条）
   - 排行榜有 60s transient 缓存（v1.2.4+ 特征）
3. 线上主题 style.css Version: 1.0.1 —— 主题也不是最新（v1.2.5 应显示该版本号）
4. 但首页 header 已出现 g99-login —— 主题 header.php 部分是新的？
   结论：用户可能只覆盖了部分文件，或上传了旧包

关键结论：**用户没有成功更新插件到 v1.2.4+**。线上插件无 ip_hash 列/逻辑。
用户"积分还是有问题"的直接原因：
- 他登录玩时，若 iframe 内 SDK 加载失败或 nonce 过期，成绩未入库
- 或者成绩入库了但他是游客身份（iframe 里没带登录 cookie？同域应该带）

下一步行动：
1. 让用户确认后台插件列表中 "997426 游戏核心" 的版本号
2. 提供更可靠的更新方式：宝塔文件管理器直接解压覆盖
3. 验证方式：更新后 leaderboard 中同一游客应只有一条
